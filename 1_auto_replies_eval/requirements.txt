# Auto-Reply Evaluation Pipeline Dependencies
# 使用方法: pip install -r requirements.txt

# 核心
python-dotenv>=1.0.0

# LLM API (Real 模式时选装)
openai>=1.0.0        # OpenAI / 兼容 API (含 Anthropic 兼容端点)
anthropic>=0.30.0    # Anthropic Claude API

# 配置
pyyaml>=6.0

# 数据处理
pandas>=2.0.0

# 报告生成 (文本分析辅助)
numpy>=1.24.0
